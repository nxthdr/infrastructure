# ansible-playbook -i inventory/ --ask-become-pass playbooks/install-core-firewall.yml
#
# Manages host-level firewall rules for the core server.
# Note: Docker 27+ IP isolation for the dmz network is handled via
# com.docker.network.bridge.ip_isolation=false in terraform/coreams01.tf.
---
- hosts: core
  become: yes
  tasks: []
