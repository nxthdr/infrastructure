# WARNING
# This file is generated by render/ scripts and should not be edited directly.
#

// DNS provider
porkbun_api_key =  "{{ porkbun.api_key }}"
porkbun_secret_api_key = "{{ porkbun.secret_api_key }}"

// Vultr provider
vultr_api_key = "{{ vultr.api_key }}"

// Alertmanager
alertmanager_discord_webhook_url = "{{ alertmanager_discord_webhook_url }}"

// CHbot
chbot_discord_token = "{{ chbot_discord_token }}"

// DynDNS
dyndns_auth_token = "{{ dyndns_auth_token }}"

// Saimiris Gateway agent key
saimiris_agent_key = "{{ saimiris.agent_key }}"

// Peerlab Gateway agent key
peerlab_agent_key = "{{ peerlab.agent_key }}"

// Peerlab Gateway Auth0 M2M credentials
peerlab_m2m_app_id = "{{ peerlab.m2m_app_id }}"
peerlab_m2m_app_secret = "{{ peerlab.m2m_app_secret }}"

// Saimiris Kafka configuration
saimiris_redpanda_username = "{{ redpanda.saimiris.username }}"
saimiris_redpanda_password = "{{ redpanda.saimiris.password }}"

// Pesto Kafka configuration
pesto_redpanda_username = "{{ redpanda.pesto.username }}"
pesto_redpanda_password = "{{ redpanda.pesto.password }}"

// PostgreSQL
postgresql_username = "{{ postgresql.username }}"
postgresql_password = "{{ postgresql.password }}"

// Headscale
headscale_authkey = "{{ headscale.authkey }}"
headscale_api_key = "{{ headscale.api_key }}"
