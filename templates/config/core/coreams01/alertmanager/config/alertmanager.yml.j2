route:
  group_by: ['alertname', 'job']

  group_wait: 30s
  group_interval: 5m
  repeat_interval: 3h

  receiver: matrix

receivers:
- name: matrix
  webhook_configs:
  - url: {{ alertmanager.hookshot_webhook_url }}
